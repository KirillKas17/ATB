# ATB Dashboard - Инструкция по запуску

## 🚀 Быстрый запуск

### Windows
1. **Двойной клик** на файл `start_dashboard_with_test.bat`
2. Дождитесь автоматической проверки и запуска
3. Дашборд откроется в браузере по адресу `http://localhost:8000`

### Альтернативный запуск
```bash
# Простой запуск
start_dashboard.bat

# Запуск с проверкой данных
start_dashboard_with_test.bat

# Ручной запуск Python скрипта
python run_dashboard.py
```

## 📊 Функциональность дашборда

### Основные секции
- **Обзор** - Статус системы, баланс, торговые метрики
- **Производительность** - Детальная статистика эффективности
- **Позиции** - Активные торговые позиции
- **Аналитика** - Рыночные индикаторы и AI сигналы

### API Endpoints
- `GET /` - Главная страница дашборда
- `GET /api/status` - Статус системы
- `GET /api/trading` - Торговые данные
- `GET /api/positions` - Активные позиции
- `GET /api/analytics` - Аналитические данные
- `GET /api/health` - Проверка здоровья API
- `WS /ws` - WebSocket для real-time данных

## 🔧 Проверка передачи данных

### Автоматическая проверка
При запуске `start_dashboard_with_test.bat` автоматически выполняется:
1. Проверка доступности сервера
2. Тестирование всех API endpoints
3. Проверка WebSocket соединения
4. Валидация структуры данных
5. Тестирование производительности

### Ручная проверка
```bash
# Запуск тестирования
python test_dashboard_data.py

# Проверка конкретного URL
python test_dashboard_data.py http://localhost:8000
```

### Результаты тестирования
Результаты сохраняются в файл `dashboard_test_results_YYYYMMDD_HHMMSS.json` с подробной информацией о:
- Статусе каждого endpoint
- Времени отклика
- Структуре данных
- Ошибках и предупреждениях

## 📁 Структура файлов

```
ATB/
├── start_dashboard.bat              # Простой запуск
├── start_dashboard_with_test.bat    # Запуск с проверкой
├── run_dashboard.py                 # Python сервер
├── test_dashboard_data.py          # Тестирование данных
├── interfaces/presentation/dashboard/
│   ├── index.html                   # Главная страница
│   ├── dashboard.js                 # JavaScript логика
│   ├── style.css                    # Стили
│   └── monitoring_dashboard.py      # Dash дашборд
└── DASHBOARD_README.md             # Эта инструкция
```

## 🛠️ Требования

### Системные требования
- Windows 10/11
- Python 3.8+
- 4GB RAM
- 1GB свободного места

### Python зависимости
```
fastapi
uvicorn
aiohttp
websockets
```

## 🔍 Диагностика проблем

### Сервер не запускается
1. Проверьте наличие Python в системе
2. Убедитесь что порт 8000 свободен
3. Проверьте права доступа к папке

### Данные не отображаются
1. Проверьте консоль браузера (F12)
2. Убедитесь что API endpoints отвечают
3. Проверьте WebSocket соединение

### Ошибки в логах
```bash
# Просмотр логов сервера
python run_dashboard.py

# Проверка API
curl http://localhost:8000/api/health

# Тестирование WebSocket
python test_dashboard_data.py
```

## 📈 Мониторинг производительности

### Метрики API
- Время отклика endpoints
- Количество активных соединений
- Статус WebSocket соединений
- Использование памяти и CPU

### Рекомендации
- Мониторьте логи сервера
- Регулярно запускайте тестирование
- Проверяйте доступность всех endpoints
- Следите за производительностью WebSocket

## 🔄 Обновление данных

### Real-time обновления
- WebSocket соединение обновляется каждые 5 секунд
- HTTP endpoints обновляются по запросу
- Графики обновляются автоматически

### Ручное обновление
- Кнопка "Обновить" в секции позиций
- Перезагрузка страницы
- Переподключение WebSocket

## 🎨 Кастомизация

### Изменение стилей
Редактируйте файл `interfaces/presentation/dashboard/style.css`

### Добавление новых секций
1. Добавьте HTML в `index.html`
2. Добавьте JavaScript логику в `dashboard.js`
3. Создайте соответствующий API endpoint

### Модификация данных
Редактируйте `MOCK_DATA` в `run_dashboard.py` для изменения тестовых данных

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Запустите диагностику: `python test_dashboard_data.py`
3. Проверьте системные требования
4. Убедитесь что все файлы на месте

## 🔒 Безопасность

### Рекомендации
- Не открывайте порт 8000 в интернет
- Используйте HTTPS в продакшене
- Ограничьте доступ к API endpoints
- Мониторьте WebSocket соединения

### Продакшен настройки
```python
# В run_dashboard.py
uvicorn.run(
    app,
    host="127.0.0.1",  # Только локальный доступ
    port=8000,
    log_level="warning",  # Меньше логов
    reload=False
)
```