# 🚀 ATB Trading System Launcher v2.0

**Единая точка входа для запуска всей торговой системы ATB**

## ✨ **Что это?**

Это полнофункциональный системный launcher, который **одним кликом** поднимает всю инфраструктуру ATB Trading:

🔥 **База данных** → **Торговый движок** → **API сервер** → **Дашборд** → **Мониторинг**

---

## 🎯 **3 СПОСОБА ЗАПУСКА**

### 1. 🖱️ **Один клик - BAT файл** (Рекомендуется)
```bash
# Просто двойной клик на файл:
ATB_START.bat
```

### 2. 🐍 **Python скрипт**
```bash
python atb_launcher.py
```

### 3. 📦 **EXE файл** (После компиляции)
```bash
# Сначала создать exe:
python create_exe.py

# Затем запустить:
START_ATB_EXE.bat
```

---

## 🔧 **Автоматическая настройка**

Launcher **автоматически**:

✅ **Проверяет Python** и версию  
✅ **Устанавливает зависимости** (numpy, pandas, matplotlib)  
✅ **Создает необходимые папки** (logs, data, config, temp, backups)  
✅ **Инициализирует базу данных** SQLite  
✅ **Запускает все компоненты** в правильном порядке  
✅ **Мониторит статус** и перезапускает при сбоях  
✅ **Логирует все операции**  

---

## 📊 **Компоненты системы**

| Компонент | Описание | Порт | Статус |
|-----------|----------|------|--------|
| **Database** | SQLite база данных | - | ✅ Auto |
| **Trading Engine** | Торговый движок | - | ✅ Auto |
| **API Server** | REST API | 8000 | ✅ Auto |
| **Dashboard** | GUI интерфейс | - | ✅ Auto |
| **Monitoring** | Система мониторинга | 9090 | ✅ Auto |
| **Redis** | Кэширование | 6379 | ⚠️ Optional |

---

## ⚙️ **Конфигурация**

Все настройки в файле **`launcher_config.json`**:

```json
{
  "auto_start_components": [
    "database",
    "trading_engine", 
    "dashboard"
  ],
  "startup_delay": 2,
  "health_check_interval": 30,
  "auto_restart": true,
  "environment": "development"
}
```

### 🎛️ **Основные настройки**

- **`auto_start_components`** - Какие компоненты запускать
- **`startup_delay`** - Задержка между запусками (сек)
- **`health_check_interval`** - Интервал проверки здоровья (сек)
- **`auto_restart`** - Автоперезапуск при сбоях
- **`environment`** - Окружение (development/production)

### 🔧 **Торговые настройки**

- **`default_mode`** - Режим по умолчанию (simulation/live)
- **`default_pairs`** - Торговые пары по умолчанию
- **`default_strategies`** - Активные стратегии
- **`risk_management`** - Параметры риск-менеджмента

---

## 📁 **Структура папок**

После запуска создается структура:

```
ATB_Trading/
├── 📁 logs/              # Логи системы
│   ├── launcher.log      # Основные логи launcher'а
│   └── startup.log       # История запусков
├── 📁 data/              # Данные
│   └── atb_trading.db    # База данных SQLite
├── 📁 config/            # Конфигурации
├── 📁 temp/              # Временные файлы
├── 📁 backups/           # Резервные копии
└── 📁 dist/              # EXE файлы (после компиляции)
```

---

## 🖥️ **Создание EXE файла**

Для создания standalone exe файла:

```bash
# 1. Установить PyInstaller
pip install pyinstaller pillow

# 2. Создать exe
python create_exe.py

# 3. Результат в папке dist/
# dist/ATB_Trading_System.exe
```

### 📦 **Результат компиляции**

- **`ATB_Trading_System.exe`** - Основной exe файл (~50-100MB)
- **`START_ATB_EXE.bat`** - Bat файл для запуска exe
- **`icon.ico`** - Иконка приложения

---

## 🔍 **Мониторинг и логи**

### 📊 **Статус системы**

Launcher показывает статус всех компонентов:

```
📊 Статус системы: 5/6 компонентов активно
✅ database - Активен
✅ trading_engine - Активен  
✅ api_server - Активен
✅ dashboard - Активен
✅ monitoring - Активен
❌ redis - Неактивен (опционально)
```

### 📝 **Логирование**

Все операции логируются в `logs/launcher.log`:

```
2024-01-15 10:30:15 | INFO | 🚀 ATB Trading System Launcher инициализирован
2024-01-15 10:30:16 | INFO | ✅ Python 3.11
2024-01-15 10:30:17 | INFO | 🚀 Запуск компонента: database
2024-01-15 10:30:18 | INFO | ✅ База данных инициализирована
```

---

## 🛠️ **Устранение неполадок**

### ❌ **Частые проблемы**

1. **"Python не найден"**
   ```bash
   # Решение: Установить Python 3.8+
   # https://www.python.org/downloads/
   # ✅ Обязательно отметить "Add Python to PATH"
   ```

2. **"Ошибка импорта numpy/pandas"**
   ```bash
   # Решение: Установить зависимости
   pip install numpy pandas matplotlib
   ```

3. **"Порт 8000 занят"**
   ```bash
   # Решение: Изменить порт в launcher_config.json
   "api_port": 8001
   ```

4. **"Доступ запрещен"**
   ```bash
   # Решение: Запустить от имени администратора
   # Или проверить антивирус
   ```

### 🔧 **Диагностика**

```bash
# Проверка статуса
curl http://localhost:8000/health

# Просмотр логов
type logs\launcher.log

# Тест компонентов
python -c "import numpy, pandas, matplotlib; print('OK')"
```

---

## 🎮 **Горячие клавиши**

| Клавиша | Действие |
|---------|----------|
| **Ctrl+C** | Остановка системы |
| **Ctrl+Break** | Принудительная остановка |

---

## 🌐 **API Endpoints**

После запуска доступны:

| Endpoint | Описание |
|----------|----------|
| `http://localhost:8000/` | Главная страница API |
| `http://localhost:8000/health` | Проверка здоровья |
| `http://localhost:8000/system/status` | Статус системы |

---

## 🚀 **Быстрый старт**

### 1️⃣ **Первый запуск**
```bash
# Скачать проект
git clone [repo_url]
cd ATB_Trading

# Запустить одним кликом  
ATB_START.bat
```

### 2️⃣ **Настройка**
- Дождаться полного запуска всех компонентов
- Открыть дашборд (запустится автоматически)
- Выбрать торговые пары и стратегии
- Начать с режима "simulation"

### 3️⃣ **Использование**
- Дашборд для управления торговлей
- API для интеграции
- Логи для диагностики
- Конфиг для настройки

---

## 🔒 **Безопасность**

✅ **По умолчанию безопасно:**
- Только локальные подключения
- Режим симуляции по умолчанию
- Нет сетевых соединений
- Логирование всех операций

⚠️ **Для продакшена:**
- Включить SSL в конфиге
- Настроить firewall
- Использовать API ключи
- Регулярные бэкапы

---

## 📞 **Поддержка**

- **Логи**: `logs/launcher.log`
- **Конфиг**: `launcher_config.json`  
- **База данных**: `data/atb_trading.db`
- **API**: `http://localhost:8000`

---

## 🎉 **Итог**

**Launcher предоставляет:**

🔥 **Один клик** → **Полная система**  
⚡ **Автонастройка** → **Готова к работе**  
🛡️ **Мониторинг** → **Стабильная работа**  
📊 **Дашборд** → **Управление торговлей**  

**Просто запустите `ATB_START.bat` и начинайте торговать! 🚀**